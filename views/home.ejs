<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Home - Gerenciador de Tarefas - Henri</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <%- include('partials/header') %>

    <main class="container py-5">
      <div class="hero text-white p-4 mb-4">
        <div class="d-flex align-items-center gap-4">
          <div>
            <h1 class="display-6">Gerencie suas tarefas com simplicidade</h1>
            <p class="lead">Crie, atualize e acompanhe suas tarefas. Tudo num lugar só.</p>
            <% if (!user) { %>
              <a href="/registrar" class="btn btn-light btn-lg me-2">Começar</a>
              <a href="/login" class="btn btn-outline-light btn-lg">Entrar</a>
            <% } else { %>
              <a href="/tarefas" class="btn btn-light btn-lg">Ver minhas tarefas</a>
            <% } %>
          </div>
          <div class="ms-auto d-none d-md-block">
            <div class="hero-icon">🗂️</div>
          </div>
        </div>
      </div>

      <div class="row g-4">
        <div class="col-md-6">
          <div class="task-card p-3">
            <h5>Organize suas prioridades</h5>
            <p class="small text-muted">Use categorias e prioridades para manter foco no que importa.</p>
          </div>
        </div>
        <div class="col-md-6">
          <div class="task-card p-3">
            <h5>Simples e rápido</h5>
            <p class="small text-muted">Interface leve para registrar tarefas em segundos.</p>
          </div>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
